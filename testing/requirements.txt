# Dominion CitizenServe Scraper - Production Requirements
# Designed for Docker deployment on VPS

# Core HTTP and data processing
httpx>=0.25.0
requests>=2.31.0
beautifulsoup4>=4.12.0

# Browser automation (Docker-compatible)
patchright>=1.40.0  # Undetected Playwright replacement

# Data processing and Excel handling
pandas>=2.1.0
openpyxl>=3.1.0

# Environment and configuration
python-dotenv>=1.0.0

# Logging and monitoring
structlog>=23.1.0

# Note: BypassV3 included as git submodule
# reCAPTCHA token generation (HTTP-based, no browser needed)